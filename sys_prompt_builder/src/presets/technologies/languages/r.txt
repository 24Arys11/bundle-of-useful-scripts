    - **R**: Follow the idioms and libraries established by the project; pipe with `|>` and prefer structured data frames over ad-hoc lists. Use a consistent visualisation approach; avoid mixing paradigms within shared code. Write reusable logic as functions; document with `roxygen2`. Always set a seed before any stochastic operation. Check data assumptions explicitly: inspect distributions, missing values, and summary statistics before modelling. Use `here::here()` for paths — never hardcode. Write automated tests with the project's testing framework. Pin package versions for reproducible environments.