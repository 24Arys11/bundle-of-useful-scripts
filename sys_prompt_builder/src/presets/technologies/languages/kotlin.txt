    - **Kotlin**: Use data classes and sealed classes over raw inheritance. Prefer coroutines and `Flow` over callbacks for async. Leverage extension functions for clean APIs; avoid utility classes. Apply null-safety correctly: prefer non-null types, restrict `!!` to provably-safe cases. Apply `when` exhaustively on sealed classes. Use `object` for singletons and avoid companion object misuse. Apply a linter and static analyser in CI. Use `@JvmField` and `@JvmStatic` only for Java interop.