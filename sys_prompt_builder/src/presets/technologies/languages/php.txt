    - **PHP**: Use PHP 8.2+ features: named arguments, enums, fibers, intersection types. Enable `declare(strict_types=1)` in every file. Use Composer for dependencies; never vendor-commit. Apply a static analysis tool at maximum strictness level. Use type hints everywhere including return types. Prefer value objects over associative arrays for structured data. Follow PSR-4 autoloading and PSR-12 coding standards. Write tests with the project's test framework; use mocking for external dependencies. Never construct SQL strings manually — use prepared statements or a query builder.