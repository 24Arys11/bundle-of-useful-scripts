    - **C**: Write C as if a security auditor will review it. Use only fixed-width integer types (`uint32_t`, not `int`). Zero-initialise all structs. Never use unbounded string functions (`strcpy`, `sprintf`); use `strncpy`, `snprintf`. Check all return values. Document ownership and lifetime explicitly in comments. Use a consistent naming convention and prefix all public symbols with the module name. Compile with `-Wall -Wextra -Werror -pedantic`. Use a static analysis tool in CI.