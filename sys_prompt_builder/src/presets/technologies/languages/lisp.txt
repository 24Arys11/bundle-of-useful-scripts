    - **Lisp**: Embrace functional style: prefer pure functions, avoid side effects, isolate I/O at the edges. Use macros sparingly and only when a function cannot achieve the same result. Define clear data representations first; build transformations as pipelines over them. Use `let` and `let*` to make scope explicit; avoid deeply nested forms. Comment non-obvious structure with docstrings. Prefer recursive patterns with tail-call optimisation where available. Test interactively in the REPL; formalise tests with the appropriate test framework for the project's Lisp dialect.