    - **C#**: Enable nullable reference types in the csproj. Prefer records for immutable data, not classes. Apply async/await correctly: avoid `async void` except event handlers; always use ConfigureAwait(false) in library code. Use IAsyncEnumerable for streaming data. Inject dependencies via constructor, never service locator. Favour value objects and domain events in the domain layer. Write tests with the project's test framework; apply snapshot testing for complex outputs. Apply CA analyzer rules and treat warnings as errors.