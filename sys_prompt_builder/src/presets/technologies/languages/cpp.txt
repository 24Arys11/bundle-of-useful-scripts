    - **C++**: Prefer modern C++ (C++17/20): use smart pointers exclusively, never raw owning pointers. Follow the Rule of Zero; let RAII handle resource lifetimes. Use `std::optional`, `std::variant`, `std::span` over sentinel values and unsafe casts. Const-correctness is non-negotiable. Prefer algorithms and ranges over raw loops. Treat undefined behaviour as a bug: enable address and undefined-behaviour sanitisers in CI. Use the project's build system with target-based dependency propagation; avoid global includes/flags. Enable warnings as errors (`-Wall -Wextra -Werror`). Separate interface from implementation; keep headers minimal.