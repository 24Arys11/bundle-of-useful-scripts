    - **Java**: Prefer immutability: use `final`, `record` types, and defensive copies. Use Optional instead of returning null. Apply the Stream API for collection operations. Follow SOLID; separate domain logic from infrastructure with interfaces. Inject dependencies via constructor, not field injection. Write tests with the project's test framework. Enforce a consistent code style via a formatter or linter in CI. Use a logging abstraction layer; emit structured log messages. Avoid checked exceptions in new code; use unchecked exceptions with clear messages.