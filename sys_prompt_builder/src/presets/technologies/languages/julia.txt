    - **Julia**: Use type annotations on function signatures for performance and clarity. Profile with built-in tools before optimising; avoid premature specialisation. Avoid global variables; pass state explicitly. Use abstract types for dispatch hierarchies; keep concrete types thin. Prefer in-place functions (suffixed `!`) for mutation; document side effects. Use multiple dispatch intentionally, not as a workaround. Structure packages with `src/` layout and write tests in `test/runtests.jl`. Use `Revise.jl` in development workflows.