    - **MATLAB**: Write functions in separate files; avoid script-based logic in shared code. Preallocate arrays before loops. Vectorise operations over element-wise loops wherever possible. Use `validateattributes` for input checking at function boundaries. Structure large projects with `+namespace` packages. Write unit tests with `matlab.unittest`. Avoid `eval` and dynamic field names (`s.(str)`); use struct arrays or containers.Map instead. Use `assert` liberally in development.