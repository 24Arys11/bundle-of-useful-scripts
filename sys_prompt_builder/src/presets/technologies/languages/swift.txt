    - **Swift**: Apply Swift's value-type-first philosophy: use structs, enums with associated values, and protocols over class hierarchies. Use `guard` for early exits to reduce nesting. Handle optionals with `if let`, `guard let`, and `??`; never force-unwrap in production. Use `async/await` and structured concurrency with actors for all concurrent work; avoid DispatchQueue in new code. Follow Swift API Design Guidelines: fluent at the call site, clear at the definition. Apply a linter to enforce style consistently. Write tests with the project's test framework; apply snapshot tests for UI. Protect mutable state with actor isolation.