    - **Rust**: Embrace the ownership model — do not fight the borrow checker, redesign. Use `Result` and `?` for error propagation; define domain errors as structured types with descriptive messages. Avoid `unwrap()` and `expect()` in library code; reserve them for provably-safe cases. Use `clippy` and `rustfmt` as enforcement, not suggestions. Prefer `Iterator` chains over manual loops. Use `Arc<Mutex<T>>` sparingly; prefer message-passing with channels. Document all public items with doc comments including examples. Apply `#[must_use]` on Result-returning functions. Use `cargo test` with doc-tests to keep examples live.