    - **Flutter**: Prefer `const` constructors everywhere possible for performance. Separate business logic from UI using the project's state management pattern; never put logic in widget build methods. Keep widgets small and focused; extract even minor subtrees into named widgets for readability and rebuild efficiency. Use `ThemeData` tokens rather than hardcoded colors and text styles. Apply null-safety strictly; avoid `!` force-unwrap outside provably-safe contexts. Test with `flutter_test`: widget tests for UI, unit tests for logic. Use `flutter analyze` and `dart fix` in CI. Handle platform differences explicitly; never assume iOS behaviour matches Android.