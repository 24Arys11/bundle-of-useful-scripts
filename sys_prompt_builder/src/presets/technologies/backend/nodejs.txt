    - **Node.js**: Use ES modules (`"type": "module"`) for new projects. Never block the event loop: offload CPU-bound work to worker threads. Handle `unhandledRejection` and `uncaughtException` explicitly; log then exit gracefully. Use structured logging with JSON output. Apply graceful shutdown: listen for SIGTERM, drain connections, then exit. Validate all external input at system boundaries with a schema validator. Use connection pooling for all database clients. Manage secrets with environment variables; never commit them. Write tests with the project's test runner at both unit and integration level.