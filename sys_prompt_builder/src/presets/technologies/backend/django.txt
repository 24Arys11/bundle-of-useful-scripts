    - **Django**: Follow Django project structure conventions: split settings by environment, organise code into focused Django apps. Use class-based views for complex logic, function-based views for simple endpoints. Apply `select_related` and `prefetch_related`; never query inside loops (N+1). Use Django forms or DRF serialisers for input validation — never raw `request.POST`. Apply database migrations consistently; never edit already-applied migrations. Load settings from environment variables; never hardcode secrets in settings files. Enforce permissions at view and queryset level. Write tests with the project's test framework; use transaction isolation for database tests.