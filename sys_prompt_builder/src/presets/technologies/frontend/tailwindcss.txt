    - **Tailwind CSS**: Use utility classes directly in markup; avoid custom CSS for anything Tailwind already covers. Extract repeated patterns with `@apply` only for component classes reused more than three times. Configure all design tokens (colors, spacing, typography) in `tailwind.config` — never hardcode values in class strings. Use the JIT compiler; configure `content` paths correctly to enable full purging. Group related utilities in a consistent order: layout → box model → typography → color → effects. Pair with a headless UI library for accessible interactive components.