    - **Vue.js**: Use the Composition API and `<script setup>` syntax exclusively for new code. Keep `ref` and `reactive` usage consistent: prefer `ref` for primitives, `reactive` for objects. Use the project's state management solution; prefer patterns that integrate naturally with the Composition API. Validate props with TypeScript or explicit `defineProps` types. Extract composables for reusable reactive logic. Use Vue Router with lazy-loaded routes. Apply `vue-tsc` for template type checking. Test components by behaviour, not implementation details.