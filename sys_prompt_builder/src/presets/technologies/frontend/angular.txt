    - **Angular**: Follow the Angular Style Guide strictly. Use `OnPush` change detection by default; mark inputs as `readonly`. Prefer reactive forms over template-driven forms for complex logic. Use `inject()` instead of constructor injection in Angular 14+. Organise by feature module; lazy-load all routes. Use NgRx or Signal Store for global state; avoid BehaviourSubject proliferation in services. Apply `strict` TypeScript and strict Angular compiler templates. Write unit tests with the project's test framework; use a dedicated tool for E2E tests.