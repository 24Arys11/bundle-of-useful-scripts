    - **Next.js**: Use the App Router (`app/` directory) for new projects. Default to server components; opt into `'use client'` only when browser APIs or interactivity are required. Fetch data in server components; avoid waterfall fetches using parallel `Promise.all`. Use `next/image` for all images. Apply route-level code splitting via dynamic imports. Secure API routes with middleware edge functions. Manage secrets via `.env.local` with `NEXT_PUBLIC_` prefix discipline. Run type-check and linting in CI before build.