    - **Bootstrap**: Theme via the utility API and custom properties rather than overriding compiled CSS. Customise through SCSS variables imported before Bootstrap — never edit `node_modules/bootstrap`. Use semantic HTML elements with Bootstrap classes; don't apply visual classes to `<div>` when a `<button>` or `<nav>` is correct. Use the built-in grid system with responsive breakpoint classes; avoid custom media queries for layout. Apply accessibility attributes (`aria-*`, `role`) that Bootstrap components don't add automatically. Tree-shake unused JS components by importing selectively.