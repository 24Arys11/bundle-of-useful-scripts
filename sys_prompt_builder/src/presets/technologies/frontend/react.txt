    - **React**: Co-locate state as low as possible; lift only when shared. Extract reusable stateful logic into custom hooks. Prefer controlled components; avoid uncontrolled refs for form state unless perf-critical. Memoise only when profiling shows a need (`useMemo`, `useCallback`, `React.memo`). Use a dedicated server-state library; avoid duplicating server data into client state stores. Keep components pure and side-effect-free; isolate effects in `useEffect` with explicit dependency arrays. Apply TypeScript strict mode. Test behaviour, not implementation details.